# Production environment variables for Vite (injected at build time)
# Update API URL to your deployed backend URL
VITE_API_BASE_URL_PROD=https://api.smartstudy.example.com
# Optional: keep dev URL for local previews if needed
VITE_API_BASE_URL=http://localhost:8000
